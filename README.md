# 1s-system-
A 41-point medium distroller on Termux, loops continuously, auto-organizes files/PDFs, deletes waste, creates folders per name, syncs with GitHub, works on small LAN with high compute power.

 it failed project 1s-syster
